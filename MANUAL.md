# دليل استخدام تطبيق التشفير (Crypto App Manual)

## 📋 جدول المحتويات

1. [نظرة عامة](#نظرة-عامة)
2. [التثبيت والإعداد](#التثبيت-والإعداد)
3. [البدء السريع](#البدء-السريع)
4. [التقنيات المتاحة](#التقنيات-المتاحة)
5. [دليل الاستخدام التفصيلي](#دليل-الاستخدام-التفصيلي)
6. [شرح التقنيات](#شرح-التقنيات)
7. [واجهة برمجة التطبيقات (API)](#واجهة-برمجة-التطبيقات-api)
8. [البنية التقنية](#البنية-التقنية)
9. [الأخطاء الشائعة وحلولها](#الأخطاء-الشائعة-وحلولها)
10. [ملاحظات مهمة](#ملاحظات-مهمة)

---

## نظرة عامة

**Crypto App** هو تطبيق ويب مبني باستخدام Flask يوفر مجموعة شاملة من تقنيات التشفير وفك التشفير والهاشينج. التطبيق مصمم للأغراض التعليمية ويستخدم مكتبات Python القياسية فقط.

### المميزات الرئيسية:
- ✅ 8 تقنيات تشفير وفك تشفير
- ✅ 2 تقنيات هاشينج
- ✅ واجهة مستخدم بسيطة وسهلة
- ✅ لا يحتاج مكتبات خارجية معقدة
- ✅ مفاتيح محددة مسبقاً (LCG-based) للتعلم

---

## التثبيت والإعداد

### المتطلبات:
- Python 3.10 أو أحدث
- Flask >= 3.0.0

### خطوات التثبيت:

1. **استنساخ المشروع:**
```bash
git clone https://github.com/omarkapil/crypto-app.git
cd crypto-app
```

2. **تثبيت المكتبات المطلوبة:**
```bash
pip install -r requirements.txt
```

3. **تشغيل التطبيق:**
```bash
python app.py
```

4. **فتح المتصفح:**
```
http://localhost:5000
```

---

## البدء السريع

### خطوات الاستخدام الأساسية:

1. **افتح الصفحة الرئيسية** - ستجد 3 أزرار:
   - 🔐 **Encryption** (التشفير)
   - 🔓 **Decryption** (فك التشفير)
   - #️⃣ **Hashing** (الهاشينج)

2. **اختر نوع العملية** - اضغط على الزر المناسب

3. **اختر التقنية** - اختر من قائمة التقنيات المتاحة

4. **أدخل النص** - اكتب النص في الحقل المخصص

5. **ابدأ العملية** - اضغط على زر "Start Encryption/Decryption/Hashing"

6. **اطلع على النتيجة** - ستظهر النتيجة مع معلومات المفتاح والـ Seed

---

## التقنيات المتاحة

### تقنيات التشفير وفك التشفير (8 تقنيات):

1. **Monoalphabetic Cipher** - تشفير الاستبدال الأحادي
2. **Columnar Transposition** - تشفير التبديل العمودي
3. **Hill Cipher** - تشفير هيل (مصفوفة 2×2)
4. **RC4** - تشفير تدفقي خفيف الوزن
5. **CBC** - تشفير الكتل بوضع CBC
6. **OFB** - تشفير الكتل بوضع OFB
7. **CTR** - تشفير الكتل بوضع CTR
8. **RSA** - تشفير المفتاح العام/الخاص

### تقنيات الهاشينج (2 تقنيات):

1. **MAC (HMAC-SHA256)** - رمز مصادقة الرسالة
2. **SHA-1** - خوارزمية هاش آمنة

---

## دليل الاستخدام التفصيلي

### 1. التشفير (Encryption)

#### الخطوات:
1. من الصفحة الرئيسية، اضغط على **Encryption**
2. اختر تقنية التشفير من القائمة
3. أدخل النص المراد تشفيره
4. اضغط **Start Encryption**
5. انسخ النص المشفر والمفتاح للاحتفاظ بهما

#### مثال:
```
النص الأصلي: Hello World
التقنية: RC4
النتيجة: Ciphertext (Base64): [نص مشفر]
         Key (hex): [مفتاح]
         Seed: 98765
```

### 2. فك التشفير (Decryption)

#### الخطوات:
1. من الصفحة الرئيسية، اضغط على **Decryption**
2. اختر نفس تقنية التشفير المستخدمة
3. أدخل النص المشفر (يجب أن يكون بنفس التنسيق الذي تم إخراجه)
4. اضغط **Start Decryption**
5. ستظهر النتيجة الأصلية

#### ملاحظات مهمة:
- **CBC, OFB, CTR**: يجب إدخال النص المشفر بصيغة hex (أرقام وA-F فقط)
- **RC4**: يجب إدخال النص المشفر بصيغة Base64
- **RSA**: يجب إدخال الأرقام المشفرة مفصولة بمسافات
- **Monoalphabetic, Columnar, Hill**: أدخل النص المشفر مباشرة

### 3. الهاشينج (Hashing)

#### الخطوات:
1. من الصفحة الرئيسية، اضغط على **Hashing**
2. اختر تقنية الهاشينج (MAC أو SHA-1)
3. أدخل النص المراد عمل hash له
4. اضغط **Start Hashing**
5. انسخ قيمة الـ hash

#### ملاحظة:
- الهاشينج عملية **أحادية الاتجاه** - لا يمكن فك التشفير
- نفس النص يعطي نفس الـ hash دائماً

---

## شرح التقنيات

### 1. Monoalphabetic Cipher (تشفير الاستبدال الأحادي)

**كيف يعمل:**
- يستبدل كل حرف بحرف آخر من الأبجدية
- المفتاح هو ترتيب جديد للأبجدية (26 حرف)

**مثال:**
```
النص: hello
المفتاح: zyxwvutsrqponmlkjihgfedcba
النتيجة: svool
```

**الاستخدام:**
- التشفير: أدخل أي نص
- فك التشفير: أدخل النص المشفر

---

### 2. Columnar Transposition (تشفير التبديل العمودي)

**كيف يعمل:**
- يرتب النص في جدول حسب كلمة مفتاح
- يعيد ترتيب الأعمدة حسب ترتيب المفتاح

**مثال:**
```
النص: HELLO WORLD
المفتاح: KEY
النتيجة: [نص معاد ترتيبه]
```

**الاستخدام:**
- التشفير: أدخل نص (يتم تجاهل المسافات والأرقام)
- فك التشفير: أدخل النص المشفر

---

### 3. Hill Cipher (تشفير هيل)

**كيف يعمل:**
- يستخدم مصفوفة 2×2 للتشفير
- يحول النص إلى أزواج من الأرقام
- يضربها في المصفوفة

**مثال:**
```
النص: HELLO
المصفوفة: [[a, b], [c, d]]
النتيجة: [أرقام مشفرة]
```

**الاستخدام:**
- التشفير: أدخل نص (يتم تجاهل المسافات)
- فك التشفير: أدخل الأرقام المشفرة

---

### 4. RC4 (Rivest Cipher 4)

**كيف يعمل:**
- تشفير تدفقي (stream cipher)
- يولد keystream من المفتاح
- يعمل XOR بين النص والـ keystream

**مثال:**
```
النص: Hello World
النتيجة: Ciphertext (Base64): [نص مشفر]
         Key (hex): [مفتاح]
```

**الاستخدام:**
- التشفير: أدخل أي نص
- فك التشفير: أدخل النص المشفر بصيغة Base64

---

### 5. CBC (Cipher Block Chaining)

**كيف يعمل:**
- تشفير الكتل (block cipher)
- كل كتلة مرتبطة بالكتلة السابقة
- حجم الكتلة: 8 بايت

**مثال:**
```
النص: Hello World
النتيجة: Ciphertext (hex): [نص hex]
         Key (hex): [مفتاح]
         IV (hex): [قيمة ابتدائية]
```

**الاستخدام:**
- التشفير: أدخل أي نص
- فك التشفير: أدخل النص المشفر بصيغة hex (بدون مسافات)

---

### 6. OFB (Output Feedback)

**كيف يعمل:**
- تشفير الكتل بوضع OFB
- يولد keystream من IV
- يعمل XOR بين النص والـ keystream

**مثال:**
```
النص: Hello World
النتيجة: Ciphertext (hex): [نص hex]
         Key (hex): [مفتاح]
         IV (hex): [قيمة ابتدائية]
```

**الاستخدام:**
- التشفير: أدخل أي نص
- فك التشفير: أدخل النص المشفر بصيغة hex

---

### 7. CTR (Counter)

**كيف يعمل:**
- تشفير الكتل بوضع CTR
- يستخدم عداد (counter) متزايد
- يولد keystream من العداد

**مثال:**
```
النص: Hello World
النتيجة: Ciphertext (hex): [نص hex]
         Key (hex): [مفتاح]
         Nonce (hex): [قيمة ابتدائية]
```

**الاستخدام:**
- التشفير: أدخل أي نص
- فك التشفير: أدخل النص المشفر بصيغة hex

---

### 8. RSA (Rivest-Shamir-Adleman)

**كيف يعمل:**
- تشفير المفتاح العام
- يستخدم أعداد أولية صغيرة (للتعلم)
- كل حرف يُشفّر بشكل منفصل

**مثال:**
```
النص: Hello
النتيجة: Ciphertext: [أرقام مفصولة بمسافات]
         Public Key (n, e): (عدد, عدد)
```

**الاستخدام:**
- التشفير: أدخل أي نص
- فك التشفير: أدخل الأرقام المشفرة مفصولة بمسافات

---

### 9. MAC (Message Authentication Code)

**كيف يعمل:**
- HMAC-SHA256
- يولد hash مع مفتاح سري
- يستخدم للتحقق من صحة الرسالة

**مثال:**
```
النص: Hello World
النتيجة: MAC (HMAC-SHA256): [hash hex]
         Key (base64): [مفتاح]
```

**الاستخدام:**
- أدخل النص المراد عمل hash له
- لا يمكن فك التشفير (عملية أحادية)

---

### 10. SHA-1 (Secure Hash Algorithm 1)

**كيف يعمل:**
- خوارزمية هاش آمنة
- يولد hash بطول 160 بت (40 حرف hex)

**مثال:**
```
النص: Hello World
النتيجة: SHA-1 Hash: [hash hex]
```

**الاستخدام:**
- أدخل النص المراد عمل hash له
- لا يمكن فك التشفير (عملية أحادية)

---

## واجهة برمجة التطبيقات (API)

### نقاط النهاية (Endpoints)

#### 1. التشفير
```
POST /api/encryption/{method_name}
```

**المعاملات:**
- `method_name`: اسم التقنية (مثل: rc4, cbc, ofb, ctr, rsa, monoalphabetic, etc.)
- Body (JSON): `{"text": "النص المراد تشفيره"}`

**مثال:**
```bash
curl -X POST http://localhost:5000/api/encryption/rc4 \
  -H "Content-Type: application/json" \
  -d '{"text": "Hello World"}'
```

**الاستجابة:**
```json
{
  "method": "RC4",
  "input_text": "Hello World",
  "output": "Ciphertext (Base64): ...\nKey (hex): ...\nSeed: 98765"
}
```

#### 2. فك التشفير
```
POST /api/decryption/{method_name}
```

**المعاملات:**
- `method_name`: اسم التقنية
- Body (JSON): `{"text": "النص المشفر"}`

**مثال:**
```bash
curl -X POST http://localhost:5000/api/decryption/cbc \
  -H "Content-Type: application/json" \
  -d '{"text": "48656c6c6f20576f726c64"}'
```

#### 3. الهاشينج
```
POST /api/hashing/{method_name}
```

**المعاملات:**
- `method_name`: mac أو sha-1
- Body (JSON): `{"text": "النص المراد عمل hash له"}`

**مثال:**
```bash
curl -X POST http://localhost:5000/api/hashing/mac \
  -H "Content-Type: application/json" \
  -d '{"text": "Hello World"}'
```

### أسماء التقنيات في API:

- `monoalphabetic` - Monoalphabetic Cipher
- `columnar-transposition` - Columnar Transposition
- `hill-cipher` - Hill Cipher
- `rc4` - RC4
- `cbc` - CBC
- `ofb` - OFB
- `ctr` - CTR
- `rsa` - RSA
- `mac` - MAC
- `sha-1` - SHA-1

---

## البنية التقنية

### هيكل المشروع:

```
crypto-app/
├── app.py                      # ملف Flask الرئيسي (Routes + API)
├── requirements.txt            # المكتبات المطلوبة
├── README.md                   # ملف README
├── FLOWCHART.md                # مخطط التدفق
├── MANUAL.md                   # هذا الملف (الدليل الشامل)
│
├── crypto_modules/             # مجلد خوارزميات التشفير
│   ├── __init__.py
│   ├── utils.py                # دوال مساعدة (LCG)
│   │
│   ├── monoalphabetic_encryption.py
│   ├── monoalphabetic_decryption.py
│   ├── columnar_encryption.py
│   ├── columnar_decryption.py
│   ├── hill_encryption.py
│   ├── hill_decryption.py
│   ├── rc4_encryption.py
│   ├── rc4_decryption.py
│   ├── cbc_encryption.py
│   ├── cbc_decryption.py
│   ├── ofb_encryption.py
│   ├── ofb_decryption.py
│   ├── ctr_encryption.py
│   ├── ctr_decryption.py
│   ├── rsa_encryption.py
│   ├── rsa_decryption.py
│   ├── mac_hashing.py
│   └── sha1_hashing.py
│
├── static/                     # الملفات الثابتة
│   ├── script.js               # JavaScript (واجهة المستخدم)
│   └── style.css               # التنسيقات
│
└── templates/                   # قوالب HTML
    ├── index.html              # الصفحة الرئيسية
    ├── encryption.html         # صفحة التشفير
    ├── decryption.html         # صفحة فك التشفير
    └── hashing.html            # صفحة الهاشينج
```

### المكونات الرئيسية:

#### 1. app.py
- يدير جميع الـ routes
- يربط التقنيات بواجهة المستخدم
- يعالج طلبات API

#### 2. crypto_modules/
- كل تقنية لها ملفان: encryption و decryption
- جميع الملفات تستخدم `process_text(text: str) -> str`
- تستخدم `utils.py` لتوليد المفاتيح

#### 3. utils.py
- **DEFAULT_SEED**: 98765 (البذرة الافتراضية)
- **generate_lcg_bytes()**: توليد بايتات عشوائية
- **generate_lcg_key_alphabet()**: توليد مفتاح أبجدي
- **generate_lcg_prime_pair()**: توليد أعداد أولية (لـ RSA)

---

## الأخطاء الشائعة وحلولها

### 1. خطأ: "Input text is empty"
**السبب:** لم يتم إدخال نص
**الحل:** تأكد من إدخال نص قبل الضغط على Start

### 2. خطأ: "Ciphertext must be hex encoded" (في CBC/OFB/CTR)
**السبب:** النص المشفر ليس بصيغة hex
**الحل:** 
- تأكد من نسخ النص المشفر كاملاً من نتيجة التشفير
- يجب أن يحتوي على أرقام و A-F فقط
- لا تضع مسافات أو رموز أخرى

### 3. خطأ: "Encryption method not supported"
**السبب:** اسم التقنية غير صحيح في API
**الحل:** استخدم الأسماء الصحيحة (rc4, cbc, ofb, ctr, etc.)

### 4. خطأ: فك التشفير لا يعطي النص الأصلي
**السبب:** 
- استخدام تقنية مختلفة
- نسخ النص المشفر بشكل خاطئ
- استخدام seed مختلف

**الحل:**
- استخدم نفس التقنية للتشفير وفك التشفير
- انسخ النص المشفر كاملاً (بما في ذلك hex/base64)
- تأكد من استخدام نفس Seed (الافتراضي: 98765)

### 5. خطأ: "Network Error" أو "Failed to fetch"
**السبب:** الخادم غير يعمل
**الحل:**
- تأكد من تشغيل `python app.py`
- تحقق من أن المنفذ 5000 متاح
- أعد تحميل الصفحة

---

## ملاحظات مهمة

### 1. المفاتيح والـ Seed
- جميع التقنيات تستخدم **Seed ثابت** (98765) لتوليد المفاتيح
- هذا يعني أن نفس النص يعطي نفس النتيجة دائماً
- **مفيد للتعلم** لكن **غير آمن للاستخدام الحقيقي**

### 2. تنسيقات الإدخال/الإخراج

#### للتشفير:
- **CBC, OFB, CTR**: النص → hex
- **RC4**: النص → Base64
- **RSA**: النص → أرقام مفصولة بمسافات
- **الباقي**: نص عادي

#### لفك التشفير:
- **CBC, OFB, CTR**: hex → النص
- **RC4**: Base64 → النص
- **RSA**: أرقام → النص
- **الباقي**: نص مشفر → نص

### 3. الأمان
⚠️ **تحذير:** هذا التطبيق للأغراض التعليمية فقط!
- المفاتيح محددة مسبقاً (غير عشوائية)
- RSA يستخدم أعداد أولية صغيرة
- لا يستخدم في التطبيقات الحقيقية

### 4. الدعم
- جميع التقنيات تعمل مع النصوص الإنجليزية
- بعض التقنيات تدعم Unicode (UTF-8)
- المسافات والأرقام قد تُتجاهل في بعض التقنيات

### 5. الأداء
- التطبيق مناسب للنصوص القصيرة والمتوسطة
- النصوص الطويلة جداً قد تستغرق وقتاً أطول
- RSA أبطأ من باقي التقنيات

---

## أمثلة عملية

### مثال 1: تشفير وفك تشفير نص بسيط

**التشفير:**
1. اختر **Encryption** → **RC4**
2. أدخل: `Hello World`
3. النتيجة: `Ciphertext (Base64): [نص]` + `Key (hex): [مفتاح]`

**فك التشفير:**
1. اختر **Decryption** → **RC4**
2. أدخل النص من `Ciphertext (Base64):` فقط (بدون التسمية)
3. النتيجة: `Hello World`

### مثال 2: استخدام CBC

**التشفير:**
1. اختر **Encryption** → **CBC**
2. أدخل: `Secret Message`
3. انسخ القيمة من `Ciphertext (hex):` (مثل: `536563726574...`)

**فك التشفير:**
1. اختر **Decryption** → **CBC**
2. أدخل القيمة hex فقط (بدون "Ciphertext (hex):")
3. النتيجة: `Secret Message`

### مثال 3: عمل Hash

**MAC:**
1. اختر **Hashing** → **MAC**
2. أدخل: `My Password`
3. النتيجة: `MAC (HMAC-SHA256): [hash]`

**SHA-1:**
1. اختر **Hashing** → **SHA-1**
2. أدخل: `My Password`
3. النتيجة: `SHA-1 Hash: [hash]`

---

## الخلاصة

هذا التطبيق يوفر بيئة تعليمية شاملة لفهم تقنيات التشفير المختلفة. استخدمه لفهم:
- كيف تعمل خوارزميات التشفير
- الفرق بين أنواع التشفير المختلفة
- كيفية استخدام المفاتيح والـ IV/Nonce
- الفرق بين التشفير والهاشينج

**استمتع بالتعلم! 🎓**

---

**آخر تحديث:** 2024
**الإصدار:** 1.0
**المطور:** Crypto App Team

